services:
  openclaw:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - SETUP_PASSWORD=test
      - OPENCLAW_STATE_DIR=/data/.openclaw
      - OPENCLAW_WORKSPACE_DIR=/data/workspace
    volumes:
      - ./.tmpdata:/data
